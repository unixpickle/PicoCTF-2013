#include <stdio.h>

int main (int argc, const char * argv[]) {
	// the argument will be exactly 100 characters
	printf("%%x (%%x)");
	int i;
	for (i = 7; i < 82; i++) {
		printf(" ");
	}
	return 0;
}
